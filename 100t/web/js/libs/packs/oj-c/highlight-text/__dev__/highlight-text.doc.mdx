import { ArgTypes, ArgsTable, Canvas, Meta, Story } from '@storybook/blocks';
import 'oj-c/highlight-text';
import * as Stories from './highlight-text.stories.ts';

<Meta of={Stories} />

# Highlight Text

The `oj-c-highlight-text` component renders text with highlighting applied.

## Import

```js
//To typecheck the element APIs, import as below.
import { CHighlightTextElement } from 'oj-c/highlight-text';

//For the transpiled javascript to load the element's module, import as below
import 'oj-c/highlight-text';
```

## Usage

Here's how to implement a basic `oj-c-highlight-text`.

<Canvas of={Stories.Basic}>
  <Story of={Stories.Basic} height={350} />
</Canvas>

## Props

<ArgsTable of="oj-c-highlight-text" />
